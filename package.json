{
  "name": "branching-demo",
  "version": "1.0.0",
  "description": "Supabase branching demo with user management CLI",
  "type": "module",
  "scripts": {
    "create-user": "npx tsx scripts/create-user.ts",
    "storage": "npx tsx scripts/storage.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  }
}
